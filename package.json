{
  "name": "nyberg-rueppel",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "bigint-crypto-utils": "^3.3.0",
    "dotenv": "^17.4.2",
    "pg": "^8.20.0"
  }
}
